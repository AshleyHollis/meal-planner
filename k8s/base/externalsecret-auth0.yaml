# =============================================================================
# ExternalSecret for Auth0 Credentials
# =============================================================================
# Syncs Auth0 application credentials from Azure Key Vault to K8s Secret
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: auth0-credentials
  namespace: meal-planner
  labels:
    app.kubernetes.io/name: meal-planner
    app.kubernetes.io/part-of: meal-planner
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: azure-keyvault
  target:
    name: auth0-credentials
    creationPolicy: Owner
  data:
    - secretKey: domain
      remoteRef:
        key: meal-planner-auth0-domain
    - secretKey: client-id
      remoteRef:
        key: meal-planner-auth0-client-id
    - secretKey: client-secret
      remoteRef:
        key: meal-planner-auth0-client-secret
    - secretKey: session-secret
      remoteRef:
        key: meal-planner-auth0-session-secret
