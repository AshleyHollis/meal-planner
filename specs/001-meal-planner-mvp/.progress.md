# Progress: 001-meal-planner-mvp

## Goal
Meal Planner MVP

## Goal Type
- Type: add
- Keywords: meal, planner, mvp

## Skill Discovery
- **communication-style**: no match
- **delegation-principle**: no match
- **interview-framework**: no match
- **reality-verification**: no match
- **smart-ralph**: no match
- **spec-workflow**: no match
- No skills matched

### Post-Research Retry
- **communication-style**: matched (keywords: planning, steps, user)
- **delegation-principle**: no match
- **interview-framework**: no match
- **reality-verification**: no match
- **smart-ralph**: matched (keywords: spec, loop, user)
- **spec-workflow**: matched (keywords: user, spec)

## Learnings

### Existing Infrastructure (CRITICAL)
- yt-summarizer (C:\Users\ashle\Source\GitHub\AshleyHollis\yt-summarizer) is the reference architecture. Constitution mandates same stack.
- Azure subscription 28aefbe7-e2af-4b4a-9ce1-92d6672c31bd already deployed with AKS, ACR (acrytsummprd.azurecr.io), Key Vault, Auth0.
- GitHub Actions: 19 workflows, 9-phase CI. Can fork/adapt for meal-planner.
- Auth0 tenant already configured with BFF pattern, test users, Google/GitHub OAuth.
- Cloudflare DNS: *.apps.ashleyhollis.com wildcard. Preview envs auto-provisioned per PR.
- .NET Aspire orchestrates local dev (Azurite, SQL Server 2025, FastAPI, Next.js, workers).
- ArgoCD + Kustomize for GitOps deployment to AKS.
- Terraform backend: Azure Blob Storage (stytsummarizertfstate).
- shared-infra repo (AshleyHollis/shared-infra) exists for shared cluster resources.

### Tech Stack (Constitutional, from yt-summarizer)
- Frontend: Next.js 16 + React 19 + TypeScript 5 + Tailwind CSS 4 → Azure Static Web Apps
- Backend: FastAPI 0.115+ + SQLAlchemy 2.0 async + Pydantic v2 + Uvicorn → AKS
- Database: Azure SQL (serverless prod), SQL Server 2025 (local). Alembic migrations.
- Workers: Python polling Azure Queue Storage. One module per worker type.
- Auth: @auth0/nextjs-auth0 4.x (BFF pattern). JWT validation on API.
- Logging: structlog + OpenTelemetry everywhere.
- Package managers: npm (frontend), uv (Python).
- Testing: pytest (Python), Vitest (frontend), Playwright (E2E). 100% pass required.
- Pre-commit: ruff, ESLint, Prettier, actionlint, yamllint, gitleaks.

### Domain Research
- No competitor combines expiry-prioritized AI meal planning + equipment-specific cooking steps + multi-shop grocery lists. Genuine white space.
- Hybrid LLM + constraint validation is the pragmatic approach for recipe generation (CHI 2025).
- Ninja Combi has NO smart connectivity/API. 14 cooking modes. Informational only.
- Inventory accuracy decay is the #1 adoption risk. Auto-deduction essential.
- Schema.org Recipe property names improve LLM structured output quality.
- Ingredients must be first-class entities for inventory math, shopping consolidation, substitution.
- Store quantities in canonical metric units (g, ml, units) internally.
- P1-P4 form the true MVP core loop; P5+ are enhancements.
- Static ingredient substitution table sufficient for MVP. AI augments at runtime.
- List-based weekly view (not calendar grid) better for mobile MVP.
- Paprika-style auto-timer detection from recipe text is gold standard for cooking UX.
- Web Speech API: no Firefox support. Chrome/Edge/Safari only. Acceptable.
- Screen Wake Lock API critical for cooking mode. Chrome 84+, Edge 84+, Safari 16.4+.
- LLM cost ~$0.05-0.15 per weekly plan generation. Acceptable for personal use.

### Requirements Phase
- MVP scoped to P1-P4 only (Inventory, AI Meal Planning, Grocery List, Customization). P5-P27 documented as out-of-scope backlog.
- Dinner-only planning for MVP. Breakfast/lunch MealSlot schema exists but defaults to "skip." Full-day planning deferred to P20.
- Single-user household for MVP. Multi-user sharing deferred to P12.
- No product/brand/shop mapping for MVP. Grocery items reference Ingredient only. Product catalogue is P18.
- Meal plan generation is async: API enqueues to Azure Queue, worker processes with LLM, frontend polls for result.
- Ingredient entity is first-class (not embedded in InventoryItem or RecipeIngredient). Enables inventory math, consolidation, and future substitution.
- Grocery list "grouped by store" is ambiguous without product catalogue. Resolved: optional preferred_store field on GroceryItem for MVP.
- Auto-deduction of inventory after cooking deferred to P14. MVP only records "cooked" status.
- Ingredient seed data (~200-300 common Australian grocery items) needed before launch. Source TBD.
- 20 API endpoints defined for MVP. All require JWT auth except health probes.
- IMPORTANT: Shared infra (AKS, ACR, Key Vault, DNS, ArgoCD) lives in AshleyHollis/shared-infra repo. Meal-planner repo only has app-specific infra (Azure SQL, SWA, storage accounts) under /infra/.

### Design Phase
- yt-summarizer uses `AddExecutable` (not `AddPythonModule`) for workers in Aspire -- workers have separate `.venv` directories and run via `__main__.py`. Meal-planner worker must follow same pattern.
- yt-summarizer DB connection supports ADO.NET connection strings from Aspire (`ConnectionStrings__<name>`) with automatic conversion to SQLAlchemy async URL. Reuse `convert_ado_connection_string` logic exactly.
- yt-summarizer models use `UNIQUEIDENTIFIER` PK type mapped via `type_annotation_map` on `Base`, not generic `UUID` column type. Critical for SQL Server compatibility.
- yt-summarizer K8s manifests use ArgoCD sync waves (namespace=0, secrets=1, API=2, workers=3) for ordered deployment. Follow same wave numbering.
- Cook-time adaptation must be synchronous (API-side LLM call) because NFR-02 requires <10s latency. Only meal plan generation goes through the worker queue.
- Auto-provisioning household on first login avoids any setup wizard. JWT `sub` claim maps to `HouseholdMember.auth0_user_id`. Household created inline during auth middleware.
- Grocery list is materialized (persisted tables) not computed on-the-fly. Required for check-off state persistence and shopping complete workflow.
- 13 database tables total. All have UUID primary keys, household_id scoping where relevant, and explicit indexes.
- Single worker (meal_plan_generator) is sufficient for MVP. Unlike yt-summarizer's 4 workers, meal-planner has only one async job type.
- LLM provider abstracted behind `LLM_PROVIDER` env var. Both Anthropic and OpenAI supported. Cost ~$0.04-0.09 per plan generation, well under NFR-10 $0.15 target.
- Ingredient seed data delivered via Alembic data migration (not separate script) so it runs automatically with `alembic upgrade head`.

## Completed Tasks
- [x] 1.1 Scaffold shared Python package structure
- [x] 1.2 Create shared config module
- [x] 1.3 Create shared logging module
- [x] 1.4 Create DB connection module
- [x] 1.5 Create SQLAlchemy Base and TimestampMixin
- [x] 1.6 Create Household and HouseholdMember models

## Current Task
Awaiting next task

## Next
Task 1.7 Create Ingredient model

### Task Planning Phase
- POC-first workflow selected (GREENFIELD project). 6 phases: POC (1A-1I), Refactoring, Testing, Infrastructure, Quality Gates, PR Lifecycle.
- Critical dependency chain: shared pkg must be built first (models, config, DB, queue), then API and worker in parallel, then Aspire, then frontend, then infra/CI last.
- yt-summarizer uses `AddPythonModule` for API but `AddExecutable` for workers (separate .venv). Meal-planner design follows same split.
- yt-summarizer shared package uses hatchling build system with `packages = ["shared"]` in pyproject.toml. Must replicate exactly.
- API pyproject.toml needs path dependency on shared: `{path = "../shared", editable = true}`.
- Workers also need path dependency on shared, plus anthropic and openai SDKs.
- Aspire AppHost.csproj needs NuGet packages: Aspire.Hosting.Python, Aspire.Hosting.NodeJs, Aspire.Hosting.SqlServer, Aspire.Hosting.Azure.Storage.
- Total task count: ~80 tasks across 6 phases. Phase 1 is ~68 tasks (55%), heaviest phase.
- POC validation checkpoint at task 1.68: API health check + frontend build. Real E2E with Aspire deferred to after infra tasks.
- Seed data split into separate migration (002) to keep schema migration (001) clean and reviewable.
- Frontend uses Auth0 v4 `@auth0/nextjs-auth0` package (BFF pattern). Not v3.
